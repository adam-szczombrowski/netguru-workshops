.col-md-8
  %h1 Editing product

  = render 'form'

  = link_to 'Show', category_product_path(category, product), class: 'btn btn-default btn-sm'

  = link_to 'Back', category_path(category), class: 'btn btn-default btn-sm'
